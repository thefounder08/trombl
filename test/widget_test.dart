// Trombl widget tests
//
// The app requires Supabase credentials at runtime — end-to-end widget tests
// that render the full widget tree are integration tests, not unit tests.
// This placeholder keeps `flutter test` green; real tests live in
// integration_test/ (not yet scaffolded).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — keeps flutter test green', () {
    expect(1 + 1, equals(2));
  });
}
